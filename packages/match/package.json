{
  "name": "@world-legends/match",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "dependencies": {
    "@world-legends/shared": "workspace:*",
    "@world-legends/types": "workspace:*",
    "@world-legends/engine": "workspace:*",
    "@world-legends/cards": "workspace:*",
    "@world-legends/collection": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
