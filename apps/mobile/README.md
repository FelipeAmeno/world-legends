# `apps/mobile`

Wrapper nativo futuro (ex: via Capacitor, `docs/01-arquitetura-geral.md`, roadmap MVP5) — reaproveita 100% dos packages de domínio e da lógica de `apps/web`; nunca duplica regra de negócio (`docs/18-monorepo-architecture-master.md`, §4).

## Status

**Placeholder shell (Tarefa T001 — bootstrap).** Nenhum wrapper nativo foi inicializado ainda.

## Próximos passos

Mesma composição de `apps/web`, com camada de UI/empacotamento nativo adicional — Fase 6 do roadmap (`docs/19-implementation-strategy-master.md`, §2), depois que `apps/web` já estiver funcional.

## Dependências

Nenhuma ainda.
