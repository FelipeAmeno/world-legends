// apps/mobile — casca de placeholder na Tarefa T001 (bootstrap).
//
// Ainda NÃO inicializado com nenhum wrapper nativo (ex: Capacitor). Esta é
// apenas a estrutura de workspace. A inicialização real reaproveita 100%
// dos packages de domínio e da lógica de `apps/web` — nunca duplica regra
// de negócio (`docs/18-monorepo-architecture-master.md`, §4).

export {};
